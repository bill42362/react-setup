{
  "name": "react-setup",
  "version": "0.0.1",
  "description": "Setup a react project.",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && NODE_ENV=production webpack --progress",
    "devbuild": "npm run clean && NODE_ENV=development webpack --progress",
    "devserver": "nodemon --exec 'babel-node src/server' --inspect ./src/server",
    "start": "babel-node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bill42362/react-setup.git"
  },
  "keywords": [
    "react",
    "server-side-rendering"
  ],
  "author": "Pi-Yin Hsiao",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/bill42362/react-setup/issues"
  },
  "homepage": "https://github.com/bill42362/react-setup#readme",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-es2015": "^6.24.1",
    "eslint": "^4.19.1",
    "eslint-plugin-react": "^7.7.0",
    "nodemon": "^1.17.3",
    "rimraf": "^2.6.2",
    "webpack": "^4.5.0",
    "webpack-cli": "^2.0.14"
  },
  "dependencies": {
    "express": "^4.16.3"
  }
}
